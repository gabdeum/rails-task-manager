<div class="container mt-4">
  <h1>My tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li>[<%= task.completed ? 'x' : '_' %>] <%= link_to task.title, task_path(task) %></li>
    <% end %>
  </ul>
</div>
